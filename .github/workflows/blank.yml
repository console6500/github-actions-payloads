name: Payload
on:
  push:
    branches: [ main ]
  release:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: cat "${GITHUB_EVENT_PATH}" | tee "${GITHUB_EVENT_NAME}".json
